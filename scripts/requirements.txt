# Scripts dependencies
# Core (shared with backend)
psycopg2-binary>=2.9.0
pgvector>=0.2.0

# Scraping
requests>=2.31.0
beautifulsoup4>=4.12.0

# Image processing (for CLIP embeddings)
Pillow>=10.0.0

# Sentence transformers (for CLIP)
sentence-transformers>=2.2.0
torch>=2.0.0

# Data processing
pandas>=2.0.0
